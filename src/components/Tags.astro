---
import TagsCard from "./TagsCard.astro";

type Props = {
  tags?: string[];
};

const { tags } = Astro.props;
---

<div class="flex gap-1 flex-wrap">
  {tags?.map(tag => (
    <TagsCard tag={tag} />
  ))}
</div>